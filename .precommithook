make build

# check if building generated changes to the readme
if (( $(git ls-files -m | grep README.md | wc -c) > 0 )); then
	echo "\nERROR: Changes to generated content; please add unstaged content to your commit."
	exit -1;
else
	exit 0;
fi